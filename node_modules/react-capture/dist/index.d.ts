import React from 'react';
export interface SnapOptions {
    file?: string;
}
export declare const useCapture: () => {
    snap: (el: React.RefObject<HTMLElement>, options?: SnapOptions | undefined) => Promise<string | true>;
};
